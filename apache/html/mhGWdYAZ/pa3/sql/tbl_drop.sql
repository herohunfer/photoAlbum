# drop all tables
# the table  should be dropped in the order according to the foreign key contraints

# firstly drop the existing tables
drop table Admin,Data, Comments, AlbumAccess,Contain,Photo,Album,User;

